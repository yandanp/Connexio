import { describe, expect, it } from "vitest";

// Placeholder agar `npm run test` hijau sebelum test riil ada (Task 3+).
describe("vitest wiring", () => {
	it("runs", () => {
		expect(1 + 1).toBe(2);
	});
});
